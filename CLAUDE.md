# hakoniwa

ブラウザで動く生態系シミュレーション（Vite + TypeScript + Canvas + Web Worker）。`main` への push で GitHub Pages に自動デプロイされる。

## ドキュメントの役割

- `docs/manual.html` — **現在の実装のモデル**。因果ループ、1 tick の処理順、各種族のライフサイクル仕様、不変条件と受け入れ基準、判断の記録。開発の出発点はここ。
- `docs/design.md` — ビジョン（「こうしたい」）。多くの項目が仮・未定。
- `src/sim/params.ts` — 数値の正。マニュアルの数値は説明用の写し。

## 開発の進め方（マニュアルをモデルとして扱う）

機能追加・変更の依頼を受けたら、コードを読む前にまず `docs/manual.html` を読み、その上で考える。

1. **位置を決める**: 因果ループのどのストック/フローを足す・変えるか、1 tick の処理順のどこに入るかを言葉にする。
2. **影響を洗う**: どの不変条件（INV-xx）に影響するか。ピラミッドの順序（上の段ほど少産少死）を崩さないか。「判断の記録」で却下・決着済みの案を蒸し返していないか。
3. **受け入れ基準を先に書く**: ミクロ（単体テストで確かめる）とマクロ（実機で観察する挙動）に分ける。モデルが変わる場合は、実装前にマニュアルの変更案をユーザーと合意する。
4. **実装**: モジュール境界を守る（草は動物を知らない、上の段が下の段を引数で受け取る）。数値は `params.ts` に置き、理由をコメントに書く。
5. **検証**: `npm run typecheck` / `npm run test` / ブラウザで観察（`.claude/launch.json` の `hakoniwa-dev`）。数千 tick の挙動は `DEFAULT_TICK_MS` を一時的に下げて早送りし、コミット前に必ず戻す。
6. **マニュアルを更新**: 同じコミットで、モデル図・処理順・パラメータ表・不変条件の状態・判断の記録を直す。確認していないことを確認済みとして書かない（状態は テスト / 実機 / 未確認 の3つ）。

## コマンド

- `npm run dev` / `npm run build` / `npm run typecheck` / `npm run test`

## 慣習

- コミットメッセージは日本語。何を変えたかに加えて、なぜそうしたか・実機で何を確認したかを書く。
